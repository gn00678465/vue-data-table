import type { Column, ColumnDef, ColumnHelper, PaginationState, RowSelectionState, Table, Updater } from "@tanstack/vue-table"

export function useDataTable<T extends Record<string, any>>(table: Table<T>): any {
  return {}
}
